PREFIX dpp: <https://example.org/dpp/>
PREFIX dct: <http://purl.org/dc/terms/>

SELECT DISTINCT ?product ?name ?score WHERE {
  GRAPH ?graph {
    ?product a dpp:Smartphone ; dct:title ?name ; dpp:repairabilityScore ?score .
    FILTER(?score >= 7)
  }
}
ORDER BY DESC(?score)
