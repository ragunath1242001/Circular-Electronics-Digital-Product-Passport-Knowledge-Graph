PREFIX dpp: <https://example.org/dpp/>
PREFIX dct: <http://purl.org/dc/terms/>

SELECT DISTINCT ?product ?name ?recycler ?recyclerName WHERE {
  GRAPH ?graph {
    ?product a dpp:Smartphone ; dct:title ?name ; dpp:recycledBy ?recycler .
    OPTIONAL { ?recycler dct:title ?recyclerName }
  }
}
