PREFIX dpp: <https://example.org/dpp/>

SELECT ?version (COUNT(DISTINCT ?passport) AS ?passports) WHERE {
  GRAPH ?graph {
    ?passport a dpp:DigitalProductPassport ; dpp:ontologyVersion ?version .
  }
}
GROUP BY ?version
ORDER BY DESC(?passports)
